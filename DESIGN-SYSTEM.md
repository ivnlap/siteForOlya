# Kiki Toony — дизайн-система

Одностраничный сайт детского YouTube-канала (песенки, обучающие мультики).
Собрано из 4 референсов в `references/`. Контент-контекст — `autor/` (превью роликов: очень
насыщенные, цветные, «мультяшные 3D» картинки; сайт должен быть спокойной оправой для них).

---

## 1. Что общего в референсах

### 1.1 Композиция и «одна большая картинка»

| | Реф | Что взято |
|---|---|---|
| 1 | Nestive (умный дом) | Одна страница = один сильный кадр. Крупный герой-объект справа, текстовый блок слева, снизу — лента шагов `01–04`. Всё остальное подчинено этому центру. |
| 2 | Globa Express (travel) | Горизонтальный слайдер карточек поверх фонового кадра: стрелки ◀ ▶, тонкий прогресс-бар, крупный счётчик `01`. |
| 3 | Regulate your mood | Кремовый холст, центрированный герой, рисованные от руки закорючки вокруг заголовка, веер цветных карточек снизу. |
| 4 | BrandLyft (5.png) | Тот же кремовый холст + галерея вертикальных карточек, уезжающая за края экрана влево-вправо, дугой. Рукописные подписи-стрелки к CTA. |

Общий скелет у всех четырёх один и тот же:

```
[ узкий nav ]
[ мелкий пилюля-бейдж соц. доказательства ]
[ ОГРОМНЫЙ заголовок в 2 строки ]
[ подзаголовок в 2–3 строки, узкая колонка ]
[ 1–2 кнопки ]
[ ГЕРОЙ-МЕДИА — вылезает за края вьюпорта ]
```

Ключевой приём во всех: **герой-медиа обрезан краем экрана**. В 1 — дом уходит вправо,
в 4 — карточки уходят влево и вправо, во 2 — слайдер уезжает за правый край. Это создаёт
ощущение «продолжения» и приглашает скроллить/тянуть.

### 1.2 Типографика

- Один семейный шрифт на всё, максимум два.
- Гротеск с высоким x-height и геометрическими формами (рефы 3, 4). Реф 1 — исключение,
  там дидон-серифом с курсивной вставкой; берём оттуда только идею **акцентного курсива
  на 1–2 словах** заголовка.
- Огромный контраст размеров: заголовок 72–96px против body 16–18px. Промежуточных
  размеров почти нет — это и даёт «дорого».
- Заголовок всегда **tight**: `line-height 1.02–1.08`, `letter-spacing -0.02em…-0.03em`.
- Подзаголовок — всегда узкая колонка (max ~520–560px) и центрирован по заголовку.
- Навигация и микро-подписи — 14–15px, вес 500, без капса (кроме кнопки в рефе 1).

### 1.3 Палитра

- **Кремовый / тёплый off-white холст** (рефы 3 и 4), не чистый белый. Реф 1 — тёплый
  шалфейно-серый. Общее: фон всегда слегка тонирован, никогда `#FFFFFF`.
- **Чернильный текст** — почти-чёрный тёплый, не `#000`.
- Цвет живёт **только в медиа и в 1–2 акцентах** (кнопка, бейдж, подпись). Интерфейс сам
  по себе почти монохромный.
- В рефе 3 и 4 есть **рамка страницы** цветом (золотая / салатовая) — страница как
  карточка, лежащая на цветной подложке.

### 1.4 Ритм блоков

- Герой ≈ 85–95vh, не 100vh: всегда видно «плечо» следующего блока.
- Дальше — секции по 120–160px вертикальных отступов, с чередованием:
  узкий контент → full-bleed медиа → узкий контент.
- Full-bleed горизонтальные ленты работают как «выдох» между текстовыми блоками.
- Внутри секции всегда одинаковая лесенка: `кикер (14px) → заголовок → тело → медиа`.

### 1.5 Работа с пространством

- Воздуха вокруг заголовка больше, чем кажется нужным. Пустота — основной материал.
- Ничего не касается краёв, кроме намеренного full-bleed медиа.
- Закорючки (рефы 3, 4) живут **в пустоте по бокам заголовка**, никогда не поверх текста.
- Карточки в веере слегка повёрнуты (±2–6°) и перекрывают друг друга — это единственный
  «беспорядок», всё остальное по сетке.
- Мелкие элементы (счётчик, прогресс-бар, номера шагов) прижаты к низу блока и работают
  как «подвал» секции.

---

## 2. Дизайн-система

### 2.1 Токены

```css
:root {
  /* ---- Цвет ---- */
  --cream-50:  #FDFBF4;   /* холст страницы */
  --cream-100: #F8F3E6;   /* поверхности, «бумага» */
  --cream-200: #EFE7D3;   /* бордеры, разделители */
  --cream-300: #E2D6BB;   /* приглушённые бордеры */

  --ink-900:   #16130E;   /* заголовки, primary-кнопка */
  --ink-700:   #3B342B;   /* body */
  --ink-500:   #6E6355;   /* вторичный текст, кикеры */
  --ink-300:   #A99C89;   /* disabled, подписи */

  /* Акценты — из превью роликов канала. В UI одновременно максимум 2. */
  --acc-coral:  #F0655A;  /* primary CTA */
  --acc-sun:    #F5B93E;  /* бейджи, рамка страницы */
  --acc-sky:    #4FB3E8;
  --acc-leaf:   #7FC24A;
  --acc-berry:  #E86FA5;
  --acc-mint:   #3FBFA8;

  --frame:      var(--acc-sun);   /* цветная рамка вокруг страницы */

  /* ---- Типографика ---- */
  --font-display: 'General Sans', 'Poppins', system-ui, sans-serif;
  --font-text:    'General Sans', system-ui, sans-serif;
  --font-hand:    'Caveat', 'Gloria Hallelujah', cursive;

  /* ---- Радиусы ---- */
  --r-xs: 8px;  --r-sm: 14px;  --r-md: 22px;
  --r-lg: 32px; --r-xl: 44px;  --r-full: 999px;

  /* ---- Тени: мягкие, тёплые, низкие ---- */
  --sh-1: 0 2px 8px rgba(22,19,14,.05);
  --sh-2: 0 8px 24px rgba(22,19,14,.07);
  --sh-3: 0 18px 48px rgba(22,19,14,.10);
  --sh-card: 0 14px 36px rgba(22,19,14,.12);

  /* ---- Анимация ---- */
  --ease: cubic-bezier(.22,.61,.36,1);
  --dur-fast: 160ms; --dur: 320ms; --dur-slow: 620ms;
}
```

### 2.2 Шрифты

| Роль | Шрифт | Применение |
|---|---|---|
| Display | **General Sans** (альтернативы: Poppins, Gilroy, Switzer) | Заголовки h1–h3 |
| Text | тот же, вес 400–500 | Body, nav, кнопки |
| Hand | **Caveat** (альтернатива: Kalam) | Только закорючки-подписи: «It's free», «Смотри тут» |

Правило: рукописный шрифт — **никогда для body и никогда для заголовка**. Только 2–5 слов
как подпись рядом со стрелкой.

### 2.3 Шкала текста

| Токен | Desktop | Mobile | Параметры |
|---|---|---|---|
| `display-xl` | 96px | 44px | w600, lh 1.02, ls −0.03em |
| `display-l` | 72px | 36px | w600, lh 1.05, ls −0.025em |
| `h2` | 48px | 30px | w600, lh 1.1, ls −0.02em |
| `h3` | 30px | 24px | w600, lh 1.2 |
| `lead` | 20px | 17px | w400, lh 1.55, `max-width: 560px` |
| `body` | 17px | 16px | w400, lh 1.6, `max-width: 68ch` |
| `small` | 15px | 14px | w500, lh 1.45 |
| `kicker` | 14px | 13px | w600, ls 0.08em, uppercase, `--ink-500` |
| `hand` | 26px | 20px | Caveat, поворот −6°…+8° |

Акцентная строка заголовка: то же начертание, но `font-style: italic` + `--acc-coral`
(идея из рефа 1: «Control *Your Home*»).

### 2.4 Сетка

```
Контейнер:  max-width 1280px
Колонки:    12
Gutter:     24px
Поля:       desktop 64px · tablet 40px · mobile 20px
Full-bleed: 100vw, вырывается из контейнера (галереи, слайдеры)
```

Ширины по умолчанию: заголовок героя ≤ 10 колонок, lead ≤ 6 колонок, текстовый блок
секции ≤ 7 колонок.

Рамка страницы (реф 3/4): весь документ обёрнут в `--cream-50` c `border-radius: 28px`
и внешним фоном `--frame`, поля 12px desktop / 0 mobile.

### 2.5 Отступы

База 4px: `4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96 · 128 · 160`

| Где | Значение |
|---|---|
| Внутри карточки | 24–32 |
| Кикер → заголовок | 20 |
| Заголовок → lead | 24 |
| Lead → кнопки | 40 |
| Между блоками внутри секции | 64 |
| Между секциями | 128 desktop / 72 mobile |
| Секция → full-bleed лента | 96 |

### 2.6 Ритм страницы (одностраничник)

```
0  NAV            плавающая пилюля, sticky, blur-фон при скролле
1  HERO           88vh · бейдж → h1 → lead → 2 CTA · закорючки по бокам
2  HERO-GALLERY   full-bleed · дугой уезжающие вертикальные карточки  ← реф 4
3  ABOUT          узкая колонка + фото автора/маскота · 2 закорючки
4  PROJECTS       full-bleed слайдер плейлистов, стрелки + прогресс + 01/06  ← реф 2
5  HOW IT WORKS   лента шагов 01–04, тонкие разделители снизу  ← реф 1
6  NUMBERS        3–4 крупных числа, максимум воздуха
7  CTA            центрированный, coral-кнопка, рукописная подпись со стрелкой
8  FOOTER         тёмный ink-900, крупная подпись-логотип, ссылки на площадки
```

Правило чередования: после каждого full-bleed блока обязательно узкий текстовый —
и наоборот. Два медийных блока подряд не ставим.

---

## 3. Компоненты

### 3.1 Nav
Плавающая пилюля по центру, `--r-full`, фон `--cream-100/85` + `backdrop-filter: blur(14px)`,
бордер `--cream-200`. Слева логотип, по центру пункты 15px/w500, справа — primary-кнопка.
При скролле > 40px: тень `--sh-2`, высота −8px.

### 3.2 Бейдж соц. доказательства
Пилюля `--acc-sun` при 18% прозрачности, текст `--ink-900` w600 15px, опционально стек
аватарок (−10px overlap, бордер 2px `--cream-50`). Над заголовком, отступ снизу 28px.

### 3.3 Кнопки

| Вариант | Фон | Текст | Радиус | Паддинг |
|---|---|---|---|---|
| Primary | `--acc-coral` | `--cream-50` | full | 18/34 |
| Dark | `--ink-900` | `--cream-50` | full | 18/34 |
| Secondary | `--cream-50` | `--ink-900` | full | 18/34, бордер `--cream-300` |
| Ghost | — | `--ink-700` | full | 14/20 |

Hover: `translateY(-2px)` + тень `--sh-2`, 160ms. Active: `translateY(0) scale(.98)`.
Опция «dashed halo» из рефа 4: пунктирный контур 2px в 8px от кнопки, цвет — фон кнопки
при 45%.

### 3.4 Закорючки (doodles)
SVG, `stroke-width 2.5`, `stroke-linecap: round`, цвет `--ink-900` при 85%.
Набор: искры-звёздочки, спираль, изогнутая стрелка, подчёркивание-росчерк, «облачко».

Правила:
- максимум **2–3 на экран**;
- живут в пустоте слева/справа от заголовка, минимум 40px до текста;
- на mobile остаются только 1, остальные `display: none`;
- анимация появления — прорисовка `stroke-dashoffset` за 700ms при входе во вьюпорт;
- рукописная подпись всегда идёт **в паре со стрелкой**, указывающей на объект.

### 3.5 Медиа-карточка (вертикальная, 9:16)
`--r-lg`, `overflow: hidden`, тень `--sh-card`, соотношение 9:16, ширина 210–260px.
В веере — `rotate(±2…6deg)` и вертикальный сдвиг ±14px.
Hover: `rotate(0) scale(1.04)`, тень `--sh-3`, длительность `--dur`.
Оверлей снизу: градиент `transparent → rgba(22,19,14,.6)`, внутри — название + длительность.

### 3.6 Горизонтальная галерея (реф 4) — блок 2
Full-bleed лента вертикальных карточек, уходящая за оба края.
- Дуга: `translateY` по параболе от центра (края опускаются на ~24px), `rotate` растёт к краям.
- Автопрокрутка-marquee 60s linear infinite, пауза на hover.
- Драг мышью/свайп + `scroll-snap-type: x proximity`.
- По краям — маски `mask-image: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent)`.
- `prefers-reduced-motion` → автопрокрутка выключается, остаётся ручной скролл.

### 3.7 Слайдер проектов (реф 2) — блок 4
Горизонтальная лента крупных карточек 3:4 поверх приглушённого фонового кадра.
Управление в одну строку под лентой:

```
( ◀ ) ( ▶ )   ──────────────────────────────      01
 44px круги    прогресс-бар 1px, заполнение ink   счётчик 56px w600
```

- Круглые кнопки: бордер 1px `--cream-300`, hover — заливка `--ink-900`, иконка кремовая.
- Прогресс-бар: `--cream-300` фон, заполнение `--ink-900`, ширина = `(i+1)/n`.
- Счётчик: активный индекс `--ink-900` 56px, `/ 06` рядом 18px `--ink-300`.
- Переход слайда 620ms `--ease`, соседние карточки при этом уходят в `scale(.94) opacity(.7)`.

### 3.8 Лента шагов (реф 1) — блок 5
4 колонки. В каждой: иконка 24px → подпись 15px → 1px разделитель `--cream-300`
→ номер `01` 14px `--ink-500`. Разделитель активного шага — `--acc-coral`, шириной 100%,
у остальных 40%. Ячейки в лёгкой рамке `--cream-200`, радиус `--r-sm`.

### 3.9 Footer
Фон `--ink-900`, текст `--cream-100`. Огромная (120px+) подпись-вордмарк на всю ширину,
над ней — колонки ссылок 15px. Соцкнопки — круги 44px с бордером `--cream-100/25`.

---

## 4. Движение

| Событие | Поведение |
|---|---|
| Появление секции | `opacity 0→1`, `translateY 24→0`, 620ms `--ease`, stagger 80ms |
| Заголовок героя | построчно, stagger 120ms |
| Закорючки | прорисовка контура 700ms, с задержкой 300ms после заголовка |
| Галерея героя | бесконечный marquee 60s + parallax по скроллу (−6%) |
| Карточка hover | `scale(1.04)` + выравнивание поворота, 320ms |
| Кнопка hover | `translateY(-2px)` + тень, 160ms |
| Nav при скролле | появление тени и уплотнение, 200ms |

Всё оборачивается в `@media (prefers-reduced-motion: reduce)` → только `opacity`.

---

## 5. Что НЕ делать

- Не использовать чистый `#FFFFFF` и `#000000`.
- Не раскрашивать интерфейс в цвета превью — цвет только в медиа + 1–2 акцента.
- Не ставить рукописный шрифт в заголовки, кнопки и body.
- Не класть закорючки поверх текста и не ставить больше 3 на экран.
- Не делать герой на 100vh — всегда оставлять «плечо» следующего блока.
- Не ставить два full-bleed медийных блока подряд.
- Не добавлять промежуточные размеры текста между `h2` и `lead` — контраст важнее плавности.
