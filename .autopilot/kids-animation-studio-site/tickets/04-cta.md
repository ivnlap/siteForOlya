# T04 — CTA в WhatsApp

Требования: R06, R13–R17 · Волна 2 · Зона: `v1-reel/`

Ровно одна кнопка. `--acc-coral`, `--cream-50`, `--r-full`, 18/34, пунктирное гало.
Inline-SVG WhatsApp слева. `target="_blank" rel="noopener"`. Рукописная подпись
`usually reply same day` со стрелкой. На <768px — залипающая внизу с safe-area,
inline-версия при этом скрывается (не дубль).

Готово, когда: на странице ровно один `<a class="cta">`; ссылка-заглушка помечена TODO;
константа URL в одном месте.
