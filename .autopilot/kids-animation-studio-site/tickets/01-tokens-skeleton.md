# T01 — Дизайн-токены, шрифты, базовый скелет

Требования: R02, R28, R35 · Волна 1 · Зона: `v1-reel/styles.css`, `assets/fonts/`

Токены `DESIGN-SYSTEM.md` §2.1 в `:root`. Шкала §2.3 через `clamp()`. Рамка страницы
`--frame`. Ни одного `#FFF`/`#000`. Шрифтовые стеки + `assets/fonts/README.md` с
инструкцией на self-host (D02). Reset, фокус-стили, `prefers-reduced-motion`-база.

Готово, когда: `:root` содержит все токены ДС; страница на кремовом холсте; рамка видна;
фокус различим на клавиатуре.
